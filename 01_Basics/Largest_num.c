#include <stdio.h>
int main (){
    int a,b,c;
    printf("Enter the three numbers :");
    scanf("%d,%d,%d", &a, &b, &c);
    if (a>b){
        if(a>c){
            else
            printf("%d is the largest number", a)
        }
    }
        }
    